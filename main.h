#ifndef MAIN
#define MAIN
#include <stdio.h>
#include <stdarg.h>
#include <unistd.h>
int _printf(const char *format, ...);
char *_strcpy(char *dest, char *src);
int _strlen(char *s);
int _putchar(char c);
#endif
